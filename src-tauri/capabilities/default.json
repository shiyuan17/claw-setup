{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the setup window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
